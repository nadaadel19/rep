on: 
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: "testing github actions before"
        run: ls
      
      - uses: actions/checkout@v3

      - name: "testing github actions after"
        run: ls



